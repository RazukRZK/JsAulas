var contador = 0
var contadorPrimeiro = 1

while(contadorPrimeiro <= 10){
    while (contador <= 10) {
        console.log(`${contadorPrimeiro} x ${contador} = ${contadorPrimeiro * contador}`);
        contador++  
    } 
    contadorPrimeiro++
}
